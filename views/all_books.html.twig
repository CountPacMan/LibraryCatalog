<!DOCTYPE html>
<html>
  <head>
    <link rel='stylesheet' href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
    <link rel='stylesheet' href='/../css/styles.css'>
    <title>Books!</title>
  </head>
  <body>
    <div class='container-fluid'>

      <!-- PAGE BANNER -->
      <div class='page-header'>
        <img src='/img/fakeu.png' alt='university logo'>
        <div class='logo-words'>
          <h1>Library Catalog</h1>
          <h5>sweet Catalog-ing</h5>
        </div>
      </div>

      {% if books is not empty %}
        <h3>Books</h3>
        {% for i in 0..books|length - 1 %}
          <ul>
            <li>
              <form action='/books/{{ books[i].getId }}' method='post' class='inlineForm'>
                <input name='_method' type='hidden' value='delete'>
                <input type='submit' class='deleteLink' value='x'>
              </form>
              <a href='/books/{{ books[i].getId }}/edit'>{{ books[i].getTitle }}</a>, written by
              {{ authors[i]|length > 1 ? 'authors' : 'author' }}
              {% for j in 0..authors[i]|length - 1 %}
                [<a href='/authors/{{ authors[i][j].getId }}'>{{ authors[i][j].getName }}</a>]
              {% endfor %}

            </li>
          </ul>
        {% endfor %}
      {% else %}
        <h3>You have no books fool!</h3>
      {% endif %}

      <a href='/'>Home</a>
    </div>
  </body>
</html>
